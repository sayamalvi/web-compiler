import { createSlice } from "@reduxjs/toolkit";
const initialState = {};
const compilerSlicer = createSlice({
  name: "compilerSlice",
  initialState,
  reducers: {},
});

export default compilerSlicer.reducer;
